# Server
PORT=3000
NODE_ENV=development

# When true, enables trust proxy.
# Set to true if you're behind a reverse proxy (e.g., Nginx, Heroku, Render).
TRUST_PROXY=true

# Comma-separated list of allowed origins.
# Example: http://localhost:5173,http://localhost:3000
CORS_ORIGINS=

# Auth/token signing (hostToken)
# In production, set this to a strong random string.
JWT_SECRET=change-me

# Daily (video/audio)
DAILY_DOMAIN=https://decisra.daily.co
DAILY_API_KEY=

# OpenAI (backend only)
# Used for Verdict AI realtime proxy.
OPENAI_API_KEY=
# Optional overrides
OPENAI_REALTIME_MODEL=gpt-4o-mini-realtime-preview
# If unset, backend derives URL from model: wss://api.openai.com/v1/realtime?model=<model>
OPENAI_REALTIME_URL=

# Model used to validate if user prompts are within scope before forwarding to Realtime.
OPENAI_SCOPE_GUARD_MODEL=gpt-4o-mini

# Contact form email delivery (backend only)
# /api/contact sends an email to CONTACT_TO_EMAIL using SMTP.
CONTACT_TO_EMAIL=Yashwanthkris153@gmail.com

# SMTP settings (example values; fill with your provider)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# From address used to send the email (must be allowed by your SMTP provider)
SMTP_FROM=
